# Core Dependencies
# NOTE: This file requires Python 3.10, 3.11, or 3.12
# Python 3.13+ is NOT supported by coqui-tts
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0,<3.0.0  # NumPy 2.x has compatibility issues with some audio libraries
scipy>=1.10.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
pydub>=0.25.0
resampy>=0.4.0
noisereduce>=3.0.0

# TTS and Voice Cloning
# NOTE: coqui-tts requires Python <3.13
# The package supports Python 3.10-3.12 only
coqui-tts>=0.24.0; python_version<'3.13'
transformers>=4.30.0
sentencepiece>=0.1.99
# phonemizer requires espeak-ng to be installed on your system
# Windows: Download from https://github.com/espeak-ng/espeak-ng/releases
# Linux: sudo apt-get install espeak-ng (Debian/Ubuntu) or sudo yum install espeak-ng (RedHat/CentOS)
# macOS: brew install espeak-ng
phonemizer>=3.2.0

# GUI Framework
PyQt6>=6.5.0
PyQt6-WebEngine>=6.5.0
pyqtgraph>=0.13.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Machine Learning
scikit-learn>=1.3.0
tensorboard>=2.13.0
# wandb>=0.15.0  # Optional: for experiment tracking - uncomment if needed

# Data Processing
pandas>=2.0.0
pyyaml>=6.0
jsonschema>=4.18.0

# Audio I/O
# PyAudio installation notes:
# Windows: Not recommended - use sounddevice instead
# Linux: Install portaudio19-dev first - sudo apt-get install portaudio19-dev python3-pyaudio
# macOS: brew install portaudio && pip install pyaudio
# Recommended: sounddevice (easier cross-platform installation)
sounddevice>=0.4.6  # Recommended alternative to PyAudio
# pyaudio>=0.2.13  # Uncomment if you prefer PyAudio over sounddevice

# Utilities
tqdm>=4.65.0
colorama>=0.4.6
python-dotenv>=1.0.0
click>=8.1.0
requests>=2.31.0

# Model Optimization (Optional)
onnx>=1.14.0
onnxruntime>=1.15.0

# GPU Acceleration (Optional - see install scripts for automatic detection)
# For NVIDIA GPUs, install separately based on your CUDA version:
# CUDA 12.6 (recommended for latest GPUs): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# CUDA 11.8 (for older drivers): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# CPU only: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# For AMD GPUs with ROCm:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.0

# macOS Metal acceleration is included in standard PyTorch installation