# Core Dependencies
# PyTorch - Install separately based on your system (see README.md)
# Uncomment ONE of the following based on your hardware:
# CPU only: pip install torch torchaudio
# NVIDIA GPU (CUDA 12.4): pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# NVIDIA GPU (CUDA 12.1): pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# AMD GPU (ROCm 6.2): pip install torch torchaudio --index-url https://download.pytorch.org/whl/rocm6.2
# macOS (CPU/Metal): pip install torch torchaudio

numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0
pydub>=0.25.0
resampy>=0.4.0
noisereduce>=3.0.0

# TTS and Voice Cloning
# Note: Original Coqui TTS is unmaintained. Using community fork for Python 3.11+ support
coqui-tts>=0.24.0  # Community-maintained fork with Python 3.11/3.12 support
transformers>=4.30.0
sentencepiece>=0.1.99
phonemizer>=3.2.0  # Note: Requires espeak-ng system package (see README)

# GUI Framework
PyQt6>=6.7.0
PyQt6-WebEngine>=6.7.0
pyqtgraph>=0.13.0

# API Server
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Machine Learning
scikit-learn>=1.3.0
tensorboard>=2.13.0
# wandb>=0.15.0  # Optional: Uncomment for experiment tracking

# Data Processing
pandas>=2.0.0
pyyaml>=6.0
jsonschema>=4.18.0

# Audio I/O
# Note: PyAudio requires system-level PortAudio library (see README)
# sounddevice is recommended as a more reliable alternative
sounddevice>=0.4.6
# pyaudio>=0.2.13  # Uncomment if you prefer PyAudio (requires portaudio19-dev)

# Utilities
tqdm>=4.65.0
colorama>=0.4.6
python-dotenv>=1.0.0
click>=8.1.0
requests>=2.31.0

# Model Optimization (Optional)
# onnx>=1.14.0
# onnxruntime>=1.15.0